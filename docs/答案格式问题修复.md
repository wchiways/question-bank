# OCS答案格式问题修复指南

## 问题描述

OCS显示"❌ 此题未完成, 可能是没有匹配的选项。"

### 典型错误案例

**问题**: Access数据库具有很多特点，下列叙述中，不是Access特点的是( )。  
**选项**: A. 关系模型 B. 层次模型 C. 网状模型 D. 面向对象模型  
**AI返回**: "列完整性约束" 或 "关系模型"  
**问题**: 答案缺少选项字母"A."，导致无法匹配

## ✅ 解决方案

### 方案1: 改进AI Prompt（已实现）

更新了AI Prompt，强调必须返回完整答案格式：

```
重要：必须返回完整答案格式！
- 如果答案是A选项，必须返回"A. 选项内容"
- 不能只返回"选项内容"或"A"
- 必须包含字母和完整文字

示例：
问题：Access数据库的特点是？
选项：A. 关系模型 B. 层次模型 C. 网状模型 D. 面向对象模型
正确答案：A. 关系模型  （而不是只返回"关系模型"）
```

### 方案2: 增强答案解析逻辑（已实现）

新的解析方法：
1. 提取JSON格式的answer字段
2. 清理多余的说明文字
3. 验证答案格式

### 方案3: 使用简化Prompt（推荐）

如果AI仍然返回不完整答案，可以在config.json中添加一个更简单的模型：

```json
{
  "ai": {
    "default_provider": "ali_bailian",
    "providers": {
      "ali_bailian": {
        "model": "qwen-turbo"  // 使用更快的模型
      }
    }
  }
}
```

## 🔧 OCS配置优化

### 方法1: 使用更宽松的匹配（推荐）

```javascript
{
  "name": "OCS题库(自建版)",
  "url": "http://localhost:8000/api/v1/query",
  "method": "get",
  "data": {
    "title": "${title}",
    "options": "${options}",
    "type": "${type}"
  },
  "handler": "return (res)=>{
    var answer = res?.data?.data;
    if (!answer) return undefined;
    
    // 如果答案不包含字母前缀，尝试匹配
    if (!answer.match(/^[A-Z][.、]/)) {
      var options = res.data.options || '${options}';
      var optList = options.split(/\s+/);
      
      for (var i = 0; i < optList.length; i++) {
        if (optList[i].indexOf(answer) !== -1) {
          return optList[i];
        }
      }
    }
    
    return answer;
  }"
}
```

### 方法2: 后端智能匹配（最佳方案）

在API端点中添加选项匹配逻辑。这个功能将在下一版本中实现。

## 📊 当前状态

✅ 已改进:
- AI Prompt强调完整答案格式
- 智能答案解析
- 多种格式支持

⚠️ 已知限制:
- AI有时仍会返回不完整答案
- 不同AI模型的输出格式可能不一致

## 🎯 临时解决方案

如果答案仍然无法匹配：

1. **检查数据库**
   ```bash
   sqlite3 question_bank.db "SELECT question, answer FROM question_answer LIMIT 10"
   ```

2. **手动修正答案**
   在数据库中直接添加完整的答案（包含字母）

3. **使用其他AI模型**
   尝试使用 "qwen-plus" 或 "qwen-max"

## 💡 建议

1. **重启服务器**应用新Prompt
2. **多测试几次**看效果
3. **查看日志**了解AI实际返回内容

---

> 下个版本将实现后端选项自动匹配功能！
